services:

  backend:
    build: ./backend
    container_name: chatpdf-backend

    ports:
      - "8000:8000"

    env_file:
      - .env

  frontend:
    build: ./frontend
    container_name: chatpdf-frontend

    ports:
      - "8501:8501"

    depends_on:
      - backend